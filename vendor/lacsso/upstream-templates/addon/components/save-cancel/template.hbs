{{yield}}
{{#if this.saving}}
  <button class="btn btn-primary btn-disabled"><i class="icon icon-spinner icon-spin"></i> {{t this.savingLabel}}</button>
{{else}}
  <button {{action "save"}} disabled={{this.saveDisabled}} class="btn {{if this.saved 'btn-success' 'btn-primary'}}">
    {{t this.btnLabel}}
  </button>
{{/if}}
{{#unless this.cancelHidden}}
  <button {{action "cancel"}} class="btn btn-link">{{t this.cancelLabel}}</button>
{{/unless}}
