<section class="well">
  <h2>
    {{#if oidcEnabled}}
      <b>{{t 'authPage.oidc.status.enabled' providerName=providerDisplayName}}</b>
    {{else}}
      <b class="text-warning">{{t 'authPage.oidc.status.disabled'}}</b>
    {{/if}}
  </h2>
  <p>
    {{#if oidcEnabled}}
      {{t 'authPage.oidc.status.enabledHelp' appName=settings.appName providerName=providerDisplayName users=numUsers groups=numGroups}}
    {{else}}
      {{t 'authPage.oidc.status.disabledHelp' appName=settings.appName}}
    {{/if}}
  </p>
</section>

{{#if oidcEnabled}}
  <section class="well">
    <h3>{{t 'authPage.oidc.summary.header'}}</h3>
    <hr>
    <dl class="dl-horizontal">
      <dt>{{t 'authPage.oidc.form.displayName.label'}}</dt>
      <dd>{{providerDisplayName}}</dd>
      <dt>{{t 'authPage.oidc.form.wellKnownUrl.label'}}</dt>
      <dd><code>{{config.wellKnownUrl}}</code></dd>
      <dt>{{t 'authPage.oidc.form.clientId.label'}}</dt>
      <dd><code>{{config.clientId}}</code></dd>
      <dt>{{t 'authPage.oidc.form.scopes.label'}}</dt>
      <dd><code>{{config.scopes}}</code></dd>
      <dt>{{t 'authPage.oidc.form.subjectClaim.label'}}</dt>
      <dd><code>{{t 'authPage.oidc.form.subjectClaim.value'}}</code></dd>
      <dt>{{t 'authPage.oidc.form.groupsClaim.label'}}</dt>
      <dd><code>{{config.groupsClaim}}</code></dd>
    </dl>
  </section>

  <section class="well">
    <h3>{{t 'authPage.oidc.identity.managementHeader'}}</h3>
    <hr>
    <p>{{t 'authPage.oidc.identity.managementHelp'}}</p>
    {{#if identityUpdateSuccess}}
      <div class="alert alert-success">
        <i class="icon icon-check"></i> {{t 'authPage.oidc.identity.managementSuccess'}}
      </div>
    {{/if}}
    {{#if testedIdentity}}
      <div class="alert alert-success">
        <i class="icon icon-check"></i>
        {{t 'authPage.oidc.test.success' identity=testedIdentityName providerName=providerDisplayName}}
      </div>
      {{partial "oidc-identity-mapping"}}
      {{top-errors errors=errors}}
      <div class="text-right r-mt15">
        <button type="button" class="btn btn-default" disabled={{testing}} {{action "manageIdentity"}}>
          {{t 'authPage.oidc.test.again'}}
        </button>
        <button type="button" class="btn btn-primary" disabled={{identityApplyDisabled}} {{action "applyIdentityMapping"}}>
          {{#if applyingIdentity}}
            <i class="icon icon-spinner icon-spin"></i> {{t 'authPage.oidc.identity.applying'}}
          {{else}}
            <i class="icon icon-link"></i> {{t 'authPage.oidc.identity.apply'}}
          {{/if}}
        </button>
      </div>
    {{else}}
      {{top-errors errors=errors}}
      <button type="button" class="btn btn-primary" disabled={{testing}} {{action "manageIdentity"}}>
        {{#if testing}}
          <i class="icon icon-spinner icon-spin"></i> {{t 'authPage.oidc.test.waiting'}}
        {{else}}
          <i class="icon icon-lock"></i> {{t 'authPage.oidc.identity.managementAction'}}
        {{/if}}
      </button>
    {{/if}}
  </section>

  {{site-access
    model=model
    collection='siteAccess.organizations'
  }}

  <section class="well">
    <h3>{{t 'authPage.oidc.switch.header'}}</h3>
    <hr>
    <p>{{t 'authPage.oidc.switch.help'}}</p>
    {{#link-to "admin-tab.auth.localauth" classNames="btn btn-primary"}}
      <i class="icon icon-exchange"></i> {{t 'authPage.oidc.switch.local'}}
    {{/link-to}}
  </section>
{{else}}
  <form autocomplete="off" {{action "save" on="submit"}}>
    <section class="well">
      <h3>{{t 'authPage.oidc.registration.header'}}</h3>
      <hr>
      <p>{{t 'authPage.oidc.registration.help'}}</p>
      <div class="form-group">
        <label>{{t 'authPage.oidc.registration.callbackUrl.label'}}</label>
        <div class="input-group">
          {{input type="text" readonly=true value=callbackUrl classNames="form-control"}}
          <span class="input-group-btn">
            {{copy-to-clipboard clipboardText=callbackUrl}}
          </span>
        </div>
        <p class="help-block">{{t 'authPage.oidc.registration.callbackUrl.help'}}</p>
      </div>
    </section>

    <section class="well">
      <h3>{{t 'authPage.oidc.form.header'}}</h3>
      <hr>
      <p>{{t 'authPage.oidc.form.help'}}</p>

      <div class="row">
        <div class="col-md-6">
          <div class="form-group">
            <label>{{t 'authPage.oidc.form.displayName.label'}}*</label>
            {{input type="text" value=config.displayName classNames="form-control" placeholder=(t 'authPage.oidc.form.displayName.placeholder')}}
            <p class="help-block">{{t 'authPage.oidc.form.displayName.help'}}</p>
          </div>
        </div>
        <div class="col-md-6">
          <div class="form-group">
            <label>{{t 'authPage.oidc.form.wellKnownUrl.label'}}*</label>
            {{input type="url" value=config.wellKnownUrl classNames="form-control" placeholder=(t 'authPage.oidc.form.wellKnownUrl.placeholder')}}
            <p class="help-block">{{t 'authPage.oidc.form.wellKnownUrl.help'}}</p>
          </div>
        </div>
      </div>

      <div class="row">
        <div class="col-md-6">
          <div class="form-group">
            <label>{{t 'authPage.oidc.form.clientId.label'}}*</label>
            {{input type="text" value=config.clientId autocomplete="off" classNames="form-control"}}
          </div>
        </div>
        <div class="col-md-6">
          <div class="form-group">
            <label>{{t 'authPage.oidc.form.clientSecret.label'}}*</label>
            {{input type="password" value=config.clientSecret autocomplete="new-password" classNames="form-control" placeholder=(if config.clientSecretSet (t 'authPage.oidc.form.clientSecret.unchanged'))}}
            <p class="help-block">{{t 'authPage.oidc.form.clientSecret.help'}}</p>
          </div>
        </div>
      </div>

      <div class="row">
        <div class="col-md-6">
          <div class="form-group">
            <label>{{t 'authPage.oidc.form.scopes.label'}}*</label>
            {{input type="text" value=config.scopes classNames="form-control" placeholder=(t 'authPage.oidc.form.scopes.placeholder')}}
            <p class="help-block">{{t 'authPage.oidc.form.scopes.help'}}</p>
          </div>
        </div>
        <div class="col-md-6">
          <div class="checkbox r-mt20">
            <label>
              {{input type="checkbox" checked=config.usePkce}}
              {{t 'authPage.oidc.form.pkce.label'}}
            </label>
            <p class="help-block">{{t 'authPage.oidc.form.pkce.help'}}</p>
          </div>
        </div>
      </div>

      <button type="button" class="btn btn-link r-pl0" {{action "toggleAdvanced"}}>
        <i class="icon {{if advancedOpen 'icon-chevron-up' 'icon-chevron-down'}}"></i>
        {{if advancedOpen (t 'authPage.oidc.form.advanced.hide') (t 'authPage.oidc.form.advanced.show')}}
      </button>

      {{#if advancedOpen}}
        <div class="well well-sm r-mt10">
          <h4>{{t 'authPage.oidc.form.claims.header'}}</h4>
          <p class="help-block">{{t 'authPage.oidc.form.claims.help'}}</p>
          <div class="row">
            <div class="col-md-4">
              <div class="form-group">
                <label>{{t 'authPage.oidc.form.subjectClaim.label'}}</label>
                {{input type="text" readonly=true value=(t 'authPage.oidc.form.subjectClaim.value') classNames="form-control"}}
                <p class="help-block">{{t 'authPage.oidc.form.subjectClaim.help'}}</p>
              </div>
            </div>
            <div class="col-md-4">
              <div class="form-group">
                <label>{{t 'authPage.oidc.form.usernameClaim.label'}}</label>
                {{input type="text" value=config.usernameClaim classNames="form-control" placeholder=(t 'authPage.oidc.form.usernameClaim.placeholder')}}
              </div>
            </div>
            <div class="col-md-4">
              <div class="form-group">
                <label>{{t 'authPage.oidc.form.displayNameClaim.label'}}</label>
                {{input type="text" value=config.displayNameClaim classNames="form-control" placeholder=(t 'authPage.oidc.form.displayNameClaim.placeholder')}}
              </div>
            </div>
          </div>
          <div class="row">
            <div class="col-md-6">
              <div class="form-group">
                <label>{{t 'authPage.oidc.form.emailClaim.label'}}</label>
                {{input type="text" value=config.emailClaim classNames="form-control" placeholder=(t 'authPage.oidc.form.emailClaim.placeholder')}}
              </div>
            </div>
            <div class="col-md-6">
              <div class="form-group">
                <label>{{t 'authPage.oidc.form.groupsClaim.label'}}</label>
                {{input type="text" value=config.groupsClaim classNames="form-control" placeholder=(t 'authPage.oidc.form.groupsClaim.placeholder')}}
                <p class="help-block">{{t 'authPage.oidc.form.groupsClaim.help'}}</p>
              </div>
            </div>
          </div>
          <div class="form-group">
            <label>{{t 'authPage.oidc.form.certificateAuthority.label'}}</label>
            {{textarea value=config.certificateAuthority rows="5" classNames="form-control" placeholder=(t 'authPage.oidc.form.certificateAuthority.placeholder')}}
            <p class="help-block">{{t 'authPage.oidc.form.certificateAuthority.help'}}</p>
          </div>
        </div>
      {{/if}}

      {{top-errors errors=errors}}

      <div class="text-center r-mt20">
        <button type="submit" class="btn btn-primary" disabled={{saving}}>
          {{#if saving}}
            <i class="icon icon-spinner icon-spin"></i>
            {{t 'generic.saving'}}
          {{else}}
            {{t 'authPage.oidc.form.save'}}
          {{/if}}
        </button>
      </div>
    </section>

    <section class="well">
      <h3>{{t 'authPage.oidc.test.header'}}</h3>
      <hr>
      {{#if testedIdentity}}
        <div class="alert alert-success">
          <i class="icon icon-check"></i>
          {{t 'authPage.oidc.test.success' identity=testedIdentityName providerName=providerDisplayName}}
        </div>
        <p>{{t 'authPage.oidc.test.activateHelp' providerName=providerDisplayName}}</p>
        {{partial "oidc-identity-mapping"}}
      {{else}}
        <p>{{t 'authPage.oidc.test.help' providerName=providerDisplayName}}</p>
      {{/if}}
      <div class="row">
        <div class="col-md-12 text-right">
          {{#if testedIdentity}}
            <button type="button" class="btn btn-default" disabled={{testDisabled}} {{action "authenticate"}}>
              {{t 'authPage.oidc.test.again'}}
            </button>
            <button type="button" class="btn btn-primary" disabled={{activateDisabled}} {{action "activate"}}>
              {{#if activating}}
                <i class="icon icon-spinner icon-spin"></i>
                {{t 'authPage.oidc.test.activating'}}
              {{else}}
                <i class="icon icon-lock"></i>
                {{t 'authPage.oidc.test.activateAction' providerName=providerDisplayName}}
              {{/if}}
            </button>
          {{else}}
          <button type="button" class="btn btn-primary" disabled={{testDisabled}} {{action "authenticate"}}>
            {{#if testing}}
              <i class="icon icon-spinner icon-spin"></i>
              {{t 'authPage.oidc.test.waiting'}}
            {{else}}
              <i class="icon icon-lock"></i>
              {{t 'authPage.oidc.test.action' providerName=providerDisplayName}}
            {{/if}}
          </button>
          {{/if}}
        </div>
      </div>
    </section>
  </form>
{{/if}}
