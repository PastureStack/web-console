{{#sortable-table
  classNames="lacsso grid sortable-table"
  bulkActions=false
  prefix=true
  paging=false
  search=false
  sortBy=this.sortBy
  headers=this.headers
  body=this.model.summary
  as |row kind dt|
}}
  {{#if (eq kind "row")}}
    <td data-title="{{dt.name}}">{{row.processName}}</td>
    <td data-title="{{dt.running}}">{{row.running}}</td>
    <td data-title="{{dt.ready}}">{{row.ready}}</td>
    <td data-title="{{dt.delay}}">{{row.delay}}</td>
  {{else if (eq kind "nomatch")}}
    <td colspan="{{this.headers.length}}" class="text-center text-muted lacsso pt-20 pb-20">{{t 'processesPage.summary.noMatch'}}</td>
  {{else if (eq kind "norows")}}
    <td colspan="{{this.headers.length}}" class="text-center text-muted lacsso pt-20 pb-20">{{t 'processesPage.summary.noData'}}</td>
  {{else if (and this.model.summary.length (eq kind "prefix"))}}
    <tr class="total">
      <td data-title="{{dt.name}}">{{this.total.processName}}</td>
      <td data-title="{{dt.running}}">{{this.total.running}}</td>
      <td data-title="{{dt.ready}}">{{this.total.ready}}</td>
      <td data-title="{{dt.delay}}">{{this.total.delay}}</td>
    </tr>
  {{/if}}
{{/sortable-table}}
