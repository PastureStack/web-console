{{#sortable-table
  classNames="lacsso grid sortable-table container-table table-fit-content"
  body=this.model.instances
  sortBy=this.sortBy
  bulkActions=false
  search=true
  columnSelector=true
  columnPreference=this.statsColumnPreferenceKey
  paging=true
  perPage=this.prefs.statsTablePerPage
  pageSizeOptions=this.statsPageSizes
  perPagePreference=this.statsTablePreferenceKey
  headers=this.headers
  liveSortFields=this.liveSortFields
  sortRevision=this.statsSortRevision
  animateLiveSort=true
  onPageContentChange=(action 'setVisibleStatsInstances') as |container kind dt visibleColumns|}}
  {{#if (eq kind "row")}}
    {{container-row
      model=container
      showStats=true
      showActions=true
      showCommand="column"
      showStateColumn=visibleColumns.state
      showNameColumn=visibleColumns.name
      showCpuColumn=visibleColumns.cpu
      showRamColumn=visibleColumns.ram
      showNetworkColumn=visibleColumns.network
      showStorageColumn=visibleColumns.storage
      showIpColumn=visibleColumns.ip
      showImageColumn=visibleColumns.image
      showCommandColumn=visibleColumns.command
      showActionsColumn=visibleColumns.actions
      cpuMax=this.cpuMax
      memoryMax=this.memoryMax
      storageMax=this.storageMax
      networkMax=this.networkMax}}
  {{else if (eq kind "nomatch")}}
    <tr><td colspan="{{visibleColumns.count}}" class="text-center text-muted lacsso pt-20 pb-20">{{t 'containersPage.index.table.body.noMatch'}}</td></tr>
  {{else if (eq kind "norows")}}
    <tr><td colspan="{{visibleColumns.count}}" class="text-center text-muted lacsso pt-20 pb-20">{{t 'hostsPage.hostPage.containersTab.table.body.noData'}}</td></tr>
  {{/if}}
{{/sortable-table}}
