{{view-edit-project
  project=this.model.project
  allProjects=this.model.all
  projectTemplates=this.availableProjectTemplates
  initialStacks=null
  showEdit=true
  editing=false
  done=(action "done")
  cancel=(action "cancel")
}}
