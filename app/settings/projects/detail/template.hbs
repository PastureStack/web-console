{{view-edit-project
  originalProject=this.model.originalProject
  project=this.model.project
  allProjects=this.model.all
  network=this.model.network
  initialStacks=this.model.stacks
  serviceChoices=this.model.serviceChoices
  policyManager=this.model.policyManager
  showEdit=this.editing
  editing=true
  tab=this.tab
  done=(action "done")
  cancel=(action "cancel")
}}
