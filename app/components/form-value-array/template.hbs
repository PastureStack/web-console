<button class="btn-circle-text" {{action "add"}}><i class="icon icon-plus-circle"/> <span>{{t this.addActionLabel}}</span></button>

{{#if this.ary.length}}
  <table class="table fixed no-lines no-top-padding tight small">
    <tr class="text-muted hidden-xs hidden-sm">
      <th>{{t this.valueLabel}}</th>
      <th width="30">&nbsp;</th>
    </tr>
    {{#each this.ary as |row|}}
      <tr>
        <td data-title="{{t this.valueLabel}}">
          {{input-paste pasted="pastedValues" class="form-control input-sm value" type="text" value=row.value placeholder=this.valuePlaceholder}}
        </td>

        <td class="text-right">
          <button class="btn btn-primary btn-sm" {{action "remove" row}}><i class="icon icon-minus"/><span class="sr-only">{{t 'generic.remove'}}</span></button>
        </td>
      </tr>
    {{/each}}
  </table>
  {{#if this.showProTip}}
    <div class="text-muted" style="font-size: 12px; margin-bottom: 12px;">
      {{t 'formValueArray.protip'}}
    </div>
  {{/if}}
{{/if}}
