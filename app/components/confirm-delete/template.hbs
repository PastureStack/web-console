<div class="protip">{{t 'confirmDelete.protip' key=this.alternateLabel}}</div>
<hr>
<div class="container-header-text">
  {{t 'confirmDelete.title'}}:
  <div class="display-name">
    {{#if (lte this.resources.length 10)}}
      {{#each this.resources as |resource|}}
          <div>{{resource.displayName}}</div>
      {{/each}}
    {{else}}
      <div>{{this.largeDeleteText}}</div>
    {{/if}}
  </div>
</div>

{{#if this.isEnvironment}}
  <div class="r-mb15">
    {{t 'confirmDelete.environmentNote' appName=this.settings.appName}}
  </div>
{{/if}}

<div class="footer-actions">
  <button {{action "confirm"}} class="btn btn-danger">{{t 'confirmDelete.confirmAction'}}</button>
  <button {{action "cancel"}} class="btn btn-link">{{t 'confirmDelete.cancelAction'}}</button>
</div>
