<div class="content-box col-md-3 col-md-height col-md-full-height">
  <label class="text-muted">{{t 'lbAddtlInfo.info'}} (<small>{{#link-to 'service' this.service.stackId this.service.id class=''}} {{t 'lbAddtlInfo.viewDetails'}} {{/link-to}}</small>)</label>
  <div class="content">
    <div class="state">
      {{badge-state model=this.service}}  {{action-menu model=this.service size='xs' class="no-wrap pull-right"}}
    </div>

    {{#if this.defaultCert}}
      <div>
        <label class="text-muted r-mt15">{{t 'lbAddtlInfo.defaultCertificate'}}</label>
        <span class="force-wrap">
          {{this.defaultCert.name}}
        </span>
      </div>
    {{/if}}

    {{#if this.secondaryCerts.length}}
      <div>
        <label class="text-muted r-mt15">{{t 'lbAddtlInfo.alternateCertificate'}}</label>
        <ul class="list-unstyled">
          {{#each this.secondaryCerts as |cert|}}
            <li><span class="force-wrap">{{cert.name}}</span></li>
          {{/each}}
        </ul>
      </div>
    {{/if}}

    <div>
      {{#if this.service.description}}
        <label class="text-muted r-mt10">{{t 'lbAddtlInfo.description'}}</label>
        {{this.service.description}}
      {{/if}}
    </div>
  </div>
</div>
<div class="content-box col-md-3 col-md-height col-md-full-height">
  <label class="text-muted">{{t 'lbAddtlInfo.containers'}} ({{this.containers.length}})</label>
  <div class="content">
    <div class="container-dots clearfix">
      <div style="display:inline-block;">
        {{#each this.containers as |instance|}}
          {{container-dot model=instance}}
        {{else}}
          <span class="text-muted">{{t 'lbAddtlInfo.noContainers'}}</span>
        {{/each}}
      </div>
      <div class="r-mt10">
        <label class="text-muted">{{t 'lbAddtlInfo.scale'}}</label>
        {{#if this.service.canScale}}
          <span class="r-ml20 r-mr20">{{this.service.scale}}</span>
          <div class="btn-group btn-group-xs">
            <button class="btn btn-default btn-xs" {{action "scaleDown" target=this.service}} disabled={{eq this.service.scale 1}}><i class="icon icon-minus"></i></button>
            <button class="btn btn-default btn-xs" {{action "scaleUp" target=this.service}}><i class="icon icon-plus"></i></button>
          </div>
        {{else}}
          {{if this.service.isGlobalScale (t 'lbAddtlInfo.global')}}
        {{/if}}
      </div>
    </div>
  </div>
</div>
<div class="content-box col-md-3 col-md-height col-md-full-height">
  <label class="text-muted">{{t 'lbAddtlInfo.ports'}}</label>
  <div class="content" style="overflow:auto;">
    <ul class="list-unstyled">
      {{#each this.service.endpointsByPort as |endpoint|}}
        {{#each endpoint.ipAddresses as |ip|}}
          <li><a href="{{if endpoint.ssl 'https' 'http'}}://{{ip}}:{{endpoint.port}}" target="_blank">{{ip}}:{{endpoint.port}}</a></li>
        {{/each}}
      {{else}}
        <li class="text-muted">{{t 'lbAddtlInfo.noPorts'}}</li>
      {{/each}}
    </ul>
  </div>
</div>
<div class="content-box col-md-3 col-md-height col-md-full-height">
  <label class="text-muted">{{t 'lbAddtlInfo.targets'}}</label>
  <div class="content" style="overflow:auto;">
    <ul class="list-unstyled">
      {{#each this.targets as |target|~}}
        <li>
          {{#if target.selector}}
            {{target.selector}}
          {{else}}
            <a href="{{href-to "service" target.service.stackId target.service.id}}">
              {{#if (not-eq this.service.stackId target.service.stackId)~}}
                {{target.service.displayStack}}/
              {{~/if~}}
              {{~target.service.displayName~}}
            </a>
          {{~/if~}}
          {{~#if target.targetPort~}}:{{target.targetPort}}{{~/if~}}
        </li>
      {{~/each}}
    </ul>
  </div>
</div>
