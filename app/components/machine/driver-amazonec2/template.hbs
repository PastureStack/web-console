<section class="horizontal-form">
  <form>
    <div class="container-fluid">
      <div class="over-hr r-mb20">
        <span>{{t 'machine.driverAmazon.accessSection'}}</span>
      </div>

      <div class="row {{if this.isStep1 'form-group'}}">
        <div class="col-md-2 form-label">
          <label class="form-control-static">{{t 'machine.driverAmazon.region.label'}}</label>
        </div>
        <div class="col-md-8">
          {{#if this.isStep1}}
            <select class="form-control" onchange={{action (mut this.amazonec2Config.region) value="target.value"}}>
              {{#each this.regionChoices as |choice|}}
                <option value={{choice}} selected={{eq this.amazonec2Config.region choice}}>{{choice}}</option>
              {{/each}}
            </select>
          {{else}}
            <div class="form-control-static">{{this.amazonec2Config.region}}</div>
          {{/if}}
        </div>
      </div>
      <div class="row {{if this.isStep1 'form-group'}}">
        <div class="col-md-2 form-label">
          <label class="form-control-static">{{t 'machine.driverAmazon.accessKey.label'}}</label>
        </div>
        <div class="col-md-8">
          {{#if this.isStep1}}
            {{input type="text" name="username" classNames="form-control" placeholder=(t 'machine.driverAmazon.accessKey.placeholder') value=this.amazonec2Config.accessKey}}
          {{else}}
            <div class="form-control-static">{{this.amazonec2Config.accessKey}}</div>
          {{/if}}
        </div>
      </div>
      <div class="row {{if this.isStep1 'form-group'}}">
        <div class="col-md-2 form-label">
          <label class="form-control-static">{{t 'machine.driverAmazon.secretKey.label'}}</label>
        </div>
        <div class="col-md-8">
          {{#if this.isStep1}}
            {{input type="password" name="password" classNames="form-control" placeholder=(t 'machine.driverAmazon.secretKey.placeholder') value=this.amazonec2Config.secretKey}}
          {{else}}
            <div class="form-control-static text-muted text-italic">{{t 'machine.driverAmazon.secretKey.provided'}}</div>
          {{/if}}
          {{#if this.isStep1}}
            <p class="help-block">{{t 'machine.driverAmazon.accessKeyHelp'}}</p>
          {{/if}}
        </div>
      </div>
    </div>
    {{#if this.isStep1}}
      {{top-errors errors=this.errors}}

      <div class="footer-actions">
        <button {{action "awsLogin"}} name="submit" class="btn btn-primary">{{t 'machine.driverAmazon.loginAction'}}</button>
        <button {{action "cancel"}} class="btn btn-link">{{t 'generic.cancel'}}</button>
      </div>
    {{/if}}
  </form>
</section>

<section class="horizontal-form {{unless this.isStep2 'hide'}}" style="padding-top: 0">
  <div class="text-center">
    <i class="icon icon-spinner icon-spin"></i> {{t 'machine.driverAmazon.loadingAvailability'}}
  </div>
</section>

<section class="horizontal-form {{unless this.isGteStep3 'hide'}}" style="padding-top: 0">
  <div class="container-fluid">
    <div class="over-hr r-mt20 r-mb20">
      <span>{{t 'machine.driverAmazon.vpcSection'}}</span>
    </div>
    <div class="row {{if this.isStep3 'form-group'}}">
      <div class="col-md-2 form-label">
        <label class="form-control-static">{{t 'machine.driverAmazon.availabilityZone'}}</label>
      </div>
      <div class="col-md-8">
        {{#if this.isStep3}}
          <select class="form-control" onchange={{action (mut this.selectedZone) value="target.value"}}>
            {{#each this.zoneChoices as |choice|}}
              <option value={{choice}} selected={{eq this.selectedZone choice}}>{{choice}}</option>
            {{/each}}
          </select>
        {{else}}
          <div class="form-control-static">
            {{this.amazonec2Config.region}}{{this.amazonec2Config.zone}}
          </div>
        {{/if}}
      </div>
    </div>
    <div class="row {{if this.isStep3 'form-group'}}">
      <div class="col-md-2 form-label">
        <label class="form-control-static">{{t 'machine.driverAmazon.subnet'}}</label>
      </div>
      <div class="col-md-8">
        {{#if this.isStep3}}
          {{#if this.subnetChoices.length}}
            {{#each this.subnetChoices as |choice|}}
              {{#if choice.isVpc}}
                <div class="radio"><label class="text-bold">
                  {{radio-button selection=this.selectedSubnet value=choice.value}} {{choice.label}}
                </label></div>
              {{else}}
                <div class="radio"><label class="text-bold" style="margin-left: 20px;">
                  {{radio-button selection=this.selectedSubnet value=choice.value}} {{choice.label}}
                </label></div>
              {{/if}}
            {{/each}}
          {{/if}}
        {{else}}
          <div class="form-control-static">
            {{#if this.amazonec2Config.subnetId}}
              {{this.amazonec2Config.subnetId}}
            {{else}}
              {{this.amazonec2Config.vpcId}}
            {{/if}}
          </div>
        {{/if}}
      </div>
    </div>
  </div>
  {{#if this.isStep3}}
    {{top-errors errors=this.errors}}

    <div class="footer-actions">
      <button {{action "selectSubnet"}} class="btn btn-primary">{{t 'machine.driverAmazon.securityGroupSelect'}}</button>
      <button {{action "cancel"}} class="btn btn-link">{{t 'generic.cancel'}}</button>
    </div>
  {{/if}}
</section>

<section class="horizontal-form {{unless this.isStep4 'hide'}}" style="padding-top: 0">
  <div class="text-center">
    <i class="icon icon-spinner icon-spin"></i> {{t 'machine.driverAmazon.loadingSecurity'}}
  </div>
</section>

<section class="horizontal-form {{unless this.isGteStep5 'hide'}}" style="padding-top: 0">
  <div class="container-fluid">
    <div class="over-hr r-mt20 r-mb20">
      <span>{{t 'machine.driverAmazon.securityGroupSection'}}</span>
    </div>

    <div class="row {{if this.isStep5 'form-group'}}">
      <div class="col-md-2 form-label">
        <label class="form-control-static">{{t 'machine.driverAmazon.securityGroup.label'}}</label>
      </div>
      <div class="col-md-8">
        {{#if this.isStep5}}
          <div class="radio r-pt10">
            <label>
              {{radio-button selection=this.whichSecurityGroup value="default"}}
              {{#if this.defaultSecurityGroup}}
                {{format-html-message 'machine.driverAmazon.securityGroup.defaultExisting' groupName=this.defaultSecurityGroupName}}
              {{else}}
                {{format-html-message 'machine.driverAmazon.securityGroup.defaultCreate' groupName=this.defaultSecurityGroupName}}
              {{/if}}
            </label>
          </div>

          {{#unless this.isCustomSecurityGroup}}
            <p class="help-block r-ml20"><a href="{{this.settings.docsBase}}/network-ports/" target="_blank" rel="noopener noreferrer">{{t 'machine.driverAmazon.portHelp.link'}}</a> {{t 'machine.driverAmazon.portHelp.text'}}</p>
          {{/unless}}

          <div class="radio">
            <label>
              {{radio-button selection=this.whichSecurityGroup value="custom"}}
              {{t 'machine.driverAmazon.securityGroup.custom'}}
            </label>
          </div>

          {{#if this.isCustomSecurityGroup}}
            <select class="form-control existing-security-groups" multiple="true" onchange={{action 'multiSecurityGroupSelect' ''}}>
              {{#each this.allSecurityGroups as |choice|}}
                <option value={{choice.name}} selected={{include this.selectedSecurityGroup choice.name}}>{{choice.name}} ({{choice.id}})</option>
              {{/each}}
            </select>

            <p style="r-m0 r-mt10">{{t 'machine.driverAmazon.needs.label' appName=this.settings.appName}}</p>
            <ul>
              <li>{{format-html-message 'machine.driverAmazon.needs.item1' appName=this.settings.appName}}</li>
              <li>{{format-html-message 'machine.driverAmazon.needs.item2' appName=this.settings.appName}}</li>
              <li>{{format-html-message 'machine.driverAmazon.needs.item3' appName=this.settings.appName}}</li>
            </ul>
          {{/if}}
        {{else}}
          <div class="form-control-static">
            {{#if (is-array this.amazonec2Config.securityGroup)}}
              <div class="comma-seperated-list">
                {{#each this.amazonec2Config.securityGroup as |group|}}
                  <span>{{group}}</span>
                {{/each}}
              </div>
            {{else}}
              {{this.amazonec2Config.securityGroup}}
            {{/if}}
          </div>
        {{/if}}
      </div>
    </div>
  </div>
  {{#if this.isStep5}}
    {{top-errors errors=this.errors}}

    <div class="footer-actions">
      <button {{action "selectSecurityGroup"}} class="btn btn-primary">{{t 'machine.driverAmazon.setInstanceAction'}}</button>
      <button {{action "cancel"}} class="btn btn-link">{{t 'generic.cancel'}}</button>
    </div>
  {{/if}}
</section>

<section class="horizontal-form {{unless this.isStep6 'hide'}}" style="padding-top: 0">
  <div class="text-center">
    <i class="icon icon-spinner icon-spin"></i>
    {{#if this.defaultSecurityGroup}}
      {{t 'machine.driverAmazon.securityGroup.updating'}}
    {{else}}
      {{t 'machine.driverAmazon.securityGroup.creating'}}
    {{/if}}
  </div>
</section>

<section class="horizontal-form {{unless this.isGteStep7 'hide'}}" style="padding-top: 0;">
  <div class="container-fluid">
    <div class="over-hr r-mt20 r-mb20">
      <span>{{t 'machine.driverAmazon.instanceSection'}}</span>
    </div>

    {{partial "host/add-common"}}

    <div class="over-hr r-mt20 r-mb20">
      <span>{{t 'machine.driverAmazon.instanceOptionsSection'}}</span>
    </div>

    <div class="row form-group">
      <div class="col-md-2 form-label">
        <label class="form-control-static">{{t 'machine.driverAmazon.instanceType.label'}}</label>
      </div>
      <div class="col-md-4">
        {{new-select
          classNames="form-control"
          value=this.amazonec2Config.instanceType
          content=this.instanceTypes
          optionValuePath="name"
          optionLabelPath="name"
          optionGroupPath="group"
        }}
      </div>
      <div class="col-md-2 form-label">
        <label class="form-control-static">{{t 'machine.driverAmazon.rootSize.label'}}</label>
      </div>
      <div class="col-md-4">
        <div class="input-group">
          {{input type="text" classNames="form-control" placeholder="" value=this.amazonec2Config.rootSize}}
          <span class="input-group-addon">{{t 'machine.driverAmazon.rootSize.unit'}}</span>
        </div>
      </div>
    </div>

    <div class="row form-group">
      <div class="col-md-2 form-label">
        <label class="form-control-static">{{t 'machine.driverAmazon.ami.label'}}</label>
      </div>
      <div class="col-md-4">
        {{input type="text" classNames="form-control" placeholder=(t 'machine.driverAmazon.ami.placeholder') value=this.amazonec2Config.ami}}
        <p class="help-block">
          <a href="{{this.settings.docsBase}}/hosts/#amazon-ec2-image-requirements" target="_blank" rel="noopener noreferrer">{{t 'machine.driverAmazon.ami.compatibilityGuide'}}</a>
        </p>
      </div>
      <div class="col-md-2 form-label">
        <label class="form-control-static">{{t 'machine.driverAmazon.sshUser.label'}}</label>
      </div>
      <div class="col-md-4">
        {{input type="text" classNames="form-control" placeholder=(t 'machine.driverAmazon.sshUser.placeholder') value=this.amazonec2Config.sshUser}}
      </div>
    </div>
    <div class="row form-group">
      <div class="col-md-2 form-label">
        <label class="form-control-static">{{t 'machine.driverAmazon.iam.label'}}</label>
      </div>
      <div class="col-md-4">
        {{input type="text" classNames="form-control" value=this.amazonec2Config.iamInstanceProfile}}
      </div>
      <div class="col-md-2 form-label">
        <label class="form-control-static">{{t 'machine.driverAmazon.privateIp.label'}}</label>
      </div>
      <div class="col-md-4">
        <div class="checkbox form-control-static">
          <label>{{input type="checkbox" checked=this.amazonec2Config.privateAddressOnly}} {{t 'machine.driverAmazon.onlyPrivate.label'}}</label>
        </div>
      </div>
    </div>
    <div class="row form-group">
      <div class="col-md-2 form-label">
        <label class="form-control-static">{{t 'machine.driverAmazon.tags.label'}}</label>
      </div>
      <div class="col-md-12">
        {{form-key-value
            initialMap=this.amazonec2Config.tags
            changed=(action (mut this.amazonec2Config.tags))
            addActionLabel="machine.driverAmazon.tags.addAction"
            keyLabel="machine.driverAmazon.tags.keyLabel"
            keyPlaceholder="machine.driverAmazon.tags.keyPlaceholder"
            valueLabel="machine.driverAmazon.tags.valueLabel"
            valuePlaceholder="machine.driverAmazon.tags.valuePlaceholder"
        }}
      </div>
    </div>

    {{#if this.model}}
      {{partial "host/add-options"}}
    {{/if}}
  </div>

  {{top-errors errors=this.errors}}
  {{save-cancel save="save" cancel="cancel"}}
</section>
