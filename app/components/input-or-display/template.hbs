{{#if this.editable}}
  {{yield}}
{{else}}
  {{#if this.value}}
    <div class={{this.classesForDisplay}}>{{this.value}}</div>
  {{else}}
    <div class="{{this.classesForDisplay}} text-muted">{{t 'generic.na'}}</div>
  {{/if}}
{{/if}}
