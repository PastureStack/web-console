<div class="row form-group">
  <div class="col-sm-12 col-md-2 form-label">
    <label class="form-control-static">{{t 'formCommand.command.label'}}</label>
  </div>

  <div class="col-sm-12 col-md-8">
    {{#input-or-display editable=this.editing value=this.instance.command}}
      {{input-command class="form-control" type="text" changed=(fn (mut this.instance.command)) initialValue=this.instance.command placeholder=(t 'formCommand.command.placeholder')}}
    {{/input-or-display}}
  </div>
</div>
<div class="row form-group">
  <div class="col-sm-12 col-md-2 form-label">
    <label class="form-control-static">{{t 'formCommand.entryPoint.label'}}</label>
  </div>
  <div class="col-sm-12 col-md-8">
    {{#input-or-display editable=this.editing value=this.instance.entryPoint}}
      {{input-command class="form-control" type="text" changed=(fn (mut this.instance.entryPoint)) initialValue=this.instance.entryPoint placeholder=(t 'formCommand.entryPoint.placeholder')}}
    {{/input-or-display}}
  </div>
</div>
<div class="row form-group">
  <div class="col-sm-12 col-md-2 form-label">
    <label class="form-control-static">{{t 'formCommand.workingDir.label'}}</label>
  </div>
  <div class="col-sm-12 col-md-4">
    {{#input-or-display editable=this.editing value=this.instance.workingDir}}
      {{input type="text" value=this.instance.workingDir classNames="form-control" placeholder=(t 'formCommand.workingDir.placeholder')}}
    {{/input-or-display}}
  </div>
  {{#if this.editing}}
    <div class="col-sm-12 col-md-1 form-label" style="text-align: left">
      <label class="form-control-static">{{t 'formCommand.user.label'}}</label>
    </div>
    <div class="col-sm-12 col-md-3">
      {{input type="text" value=this.instance.user classNames="form-control" placeholder=(t 'formCommand.user.placeholder')}}
    </div>
  {{/if}}
</div>

{{#unless this.editing}}
<div class="row form-group">
    <div class="col-sm-12 col-md-2 form-label" style="text-align: left">
      <label class="form-control-static">{{t 'formCommand.user.label'}}</label>
    </div>
    <div class="col-sm-12 col-md-8">
      {{#if this.instance.user}}
        <div class='form-control-static text-muted'>{{this.instance.user}}</div>
      {{else}}
        <div class="form-control-static text-muted">{{t 'generic.na'}}</div>
      {{/if}}
    </div>
</div>
{{/unless}}
<div class="row">
  <div class="col-sm-12 col-md-2 form-label">
    <label>{{t 'formCommand.console.label'}}</label>
  </div>

  {{#input-or-display editable=this.editing value=this.terminal.name classesForDisplay="col-sm-12 col-md-8"}}
    <div class="col-xs-12 col-sm-6 col-md-4">
      <div class="radio small">
        <label>{{radio-button selection=this.terminal.type value="both"}} {{format-html-message 'formCommand.console.both'}}</label>
      </div>
    </div>
    <div class="col-xs-12 col-sm-6 col-md-4">
      <div class="radio small">
        <label>{{radio-button selection=this.terminal.type value="interactive"}} {{format-html-message 'formCommand.console.interactive'}}</label>
      </div>
    </div>
  {{/input-or-display}}
</div>
{{#if this.editing}}
  <div class="row form-group">
    <div class="hidden-xs hidden-sm col-md-2 form-label">&nbsp;</div>
    <div class="col-xs-12 col-sm-6 col-md-4">
      <div class="radio small">
        <label>{{radio-button selection=this.terminal.type value="terminal"}} {{format-html-message 'formCommand.console.terminal'}}</label>
      </div>
    </div>
    <div class="col-xs-12 col-sm-6 col-md-4">
      <div class="radio small">
        <label>{{radio-button selection=this.terminal.type value="none"}} {{format-html-message 'formCommand.console.none'}}</label>
      </div>
    </div>
  </div>
{{/if}}

{{#if this.isService}}
  <div class="row {{unless this.editing 'form-group'}}">
    <div class="col-sm-12 col-md-2 form-label">
      <label>{{t 'formCommand.autoRestart.label'}}</label>
    </div>

    {{#if this.editing}}
      <div class="col-xs-12 col-sm-6 col-md-4">
        <div class="radio small">
          <label>{{radio-button selection=this.startOnce value=false}} {{t 'formCommand.autoRestart.startOnceFalse'}}</label>
        </div>
      </div>
      <div class="col-xs-12 col-sm-6 col-md-4">
        <div class="radio small">
          <label>{{radio-button selection=this.startOnce value=true}} {{t 'formCommand.autoRestart.startOnceTrue'}}</label>
        </div>
      </div>
    {{else}}
      {{#if this.startOnce}}
        <div>{{this.startOnce}}</div>
      {{else}}
        <div>{{t 'generic.none'}}</div>
      {{/if}}
    {{/if}}
  </div>
{{else}}
  <div class="row {{unless this.editing 'form-group'}}">
    <div class="col-sm-12 col-md-2 form-label">
      <label>{{t 'formCommand.autoRestart.label'}}</label>
    </div>

    {{#if this.editing}}
      <div class="col-xs-12 col-sm-6 col-md-4">
        <div class="radio small">
          <label>{{radio-button selection=this.restart value="no"}} {{t 'formCommand.autoRestart.no'}}</label>
        </div>
      </div>
      <div class="col-xs-12 col-sm-6 col-md-4">
        <div class="radio small">
          <label>{{radio-button selection=this.restart value="on-failure"}} {{format-html-message 'formCommand.autoRestart.onFailure'}}</label>
        </div>
      </div>
    {{else}}
      <div class="col-xs-12 col-sm-6 col-md-4">
        {{#if this.restart}}
          <div>{{this.restart}}</div>
        {{else}}
          <div>{{t 'generic.none'}}</div>
        {{/if}}
      </div>
    {{/if}}
  </div>
  {{#if this.editing}}
    <div class="row">
      <div class="hidden-xs hidden-sm col-md-2 form-label">&nbsp;</div>
      <div class="col-xs-12 col-sm-6 col-md-4">
        <div class="radio small">
          <label>{{radio-button selection=this.restart value="always"}} {{t 'formCommand.autoRestart.always'}}</label>
        </div>
      </div>
      <div class="col-xs-12 col-sm-6 col-md-4">
        <div class="radio small">
          <label>
            {{radio-button selection=this.restart value="on-failure-cond"}}
            <span class="with-input">
              {{t 'formCommand.autoRestart.onFailureCondPrefix' limit=this.restartLimit}}
              {{input-integer min=1 safeStyle="width: 60px; padding: 0 2px; display: inline-block;" class="form-control input-sm" value=this.restartLimit}}
              {{t 'formCommand.autoRestart.onFailureCondSuffix' limit=this.restartLimit}}
            </span>
          </label>
        </div>
      </div>
    </div>
  {{/if}}
{{/if}}

{{#if this.showDrainTimeout}}
  <div class="row form-group">
    <div class="col-sm-12 col-md-2 form-label">
      <label class="form-control-static">{{t 'formCommand.drainTimeout.label'}}</label>
    </div>
    <div class="col-sm-12 col-md-4">
      <div class="input-group">
        {{input-integer step=100 classNames="form-control" value=this.instance.drainTimeoutMs}}
        <span class="input-group-addon">{{t 'formCommand.drainTimeout.unit'}}</span>
      </div>
    </div>
  </div>
{{/if}}

<div class="row">
  <div class="col-sm-12 col-md-2 form-label">
    <label class="form-control-static">{{t 'formCommand.environment.label'}}</label>
  </div>

  <div class="col-sm-12 col-md-8">
    {{form-key-value
      initialMap=this.instance.environment
      changed=(fn (mut this.instance.environment))
      addActionLabel="formCommand.environment.addAction"
      keyLabel="formCommand.environment.keyLabel"
      keyPlaceholder="formCommand.environment.keyPlaceholder"
      valueLabel="formCommand.environment.valueLabel"
      valuePlaceholder="formCommand.environment.valuePlaceholder"
      allowEmptyValue=true
      editing=this.editing
    }}
  </div>
</div>

