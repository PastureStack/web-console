<section class="header clearfix">
  <h1>{{t (if this.editing 'newSecret.title.edit' 'newSecret.title.add')}}</h1>
</section>

<section class="horizontal-form container-fluid well">
  {{form-name-description
    model=this.primaryResource
    nameRequired=true
    namePlaceholder="newSecret.name.placeholder"
    descriptionPlaceholder="newSecret.description.placeholder"
  }}

  {{#unless this.editing}}
    <div class="row">
      <div class="col-xs-12">
        <label class="form-control-static">{{t 'newSecret.value.label'}}*</label>
        {{textarea-autogrow class="form-control" value=this.userValue placeholder="newSecret.value.placeholder"}}
      </div>
    </div>
  {{/unless}}
</section>

{{top-errors errors=this.errors}}
{{save-cancel editing=true save="save" cancel="cancel"}}
