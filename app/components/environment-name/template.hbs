{{#if this.loading}}
  <span class="text-muted">{{t 'generic.loading'}}</span>
{{else}}
  {{#if (and this.env this.env.displayName)}}
    {{this.env.displayName}}
  {{else}}
    <span class="text-muted">&mdash;</span>
  {{/if}}
{{/if}}
