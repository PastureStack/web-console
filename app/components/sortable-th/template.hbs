<button class="btn btn-link r-p0">
  {{#if hasBlock}}{{yield}}{{else}}{{t this.label}}{{/if}}
  <span class="icon-stack" aria-hidden="true">
    {{#if this.activeAscending}}
      <i class="icon icon-sort-down icon-stack-1x"></i>
    {{else if this.activeDescending}}
      <i class="icon icon-sort-up icon-stack-1x"></i>
    {{else}}
      <i class="icon icon-sort icon-stack-1x faded"></i>
    {{/if}}
  </span>
</button>
