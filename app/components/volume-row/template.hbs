<td>
  {{badge-state model=this.model}}
</td>

<td>
  <div class="clip">
    {{this.model.displayUri}}
    {{copy-to-clipboard clipboardText=this.model.displayUri class="with-clip" size="small"}}
  </div>
</td>

<td>
  {{#if this.model.mounts.length}}
    {{#each this.model.mounts as |mount|}}
      <div>
        {{#link-to "container" mount.instanceId}}{{mount.instance.name}}{{/link-to}}:{{mount.path}}
        {{#if (eq mount.permissions "ro")}}
          <span class="text-muted">{{t 'volumeRow.mounts.readOnly'}}</span>
        {{/if}}
      </div>
    {{/each}}
  {{else}}
    <span class="text-muted">{{t 'generic.none'}}</span>
  {{/if}}
</td>

<td class="actions">
  {{action-menu model=this.model}}
</td>
