<div class="row-same-height row-full-height">
    <ul class="nav nav-tabs nav-tabs-well shadowed">
      <li class="{{if (eq this.activeTab '') 'active'}}">
         <span class='close' {{action 'dismiss'}}><i class="icon icon-x"></i></span>
         <a href="#" class="clip" {{action 'selectTab' ''}}>{{this.service.name}}</a>
      </li>
      {{#each this.sidekicks as |sidekick|}}
        <li class="{{if (eq this.activeTab sidekick.name) 'active'}}">
          <a href="#" class="clip" {{action 'selectTab' sidekick.name}}>{{sidekick.name}}</a>
        </li>
      {{/each}}
    </ul>

    <!-- Tab panes -->
    <div class="tab-content">
      <div class="{{unless (eq this.activeTab '') 'hide'}}">
        <div class="inner">
          {{#if (eq this.service.type 'loadBalancerService')}}
            {{lb-addtl-info service=this.service containers=this.primaryContainers}}
          {{else}}
            {{#if (eq this.service.type 'dnsService')}}
              {{partial 'alias-service-addtl-info'}}
            {{else}}
              {{#if (eq this.service.type 'externalService')}}
                {{partial 'ext-service-addtl-info'}}
              {{else}}
                {{partial 'service-addtl-info'}}
              {{/if}}
            {{/if}}
          {{/if}}
        </div>
      </div>
      {{#each this.sidekicks as |sidekick|}}
        <div class="{{unless (eq this.activeTab sidekick.name) 'hide'}}">
          <div class="inner">
            {{sidekick-addtl-info sidekick=sidekick.config containers=sidekick.instances}}
          </div>
        </div>
      {{/each}}
    </div>
  {{yield}}
</div>

