<section class="horizontal-form container-fluid">
  <h2>{{t 'editAccount.title'}}</h2>
  <hr>

  <div class="row form-group r-mt20">
    <div class="col-sm-12 col-md-3 form-label">
      <label class="form-control-static">{{t 'editAccount.form.name.label'}}</label>
    </div>
    <div class="col-sm-12 col-md-7">
      {{#if this.isAdmin}}
        {{input type="text" value=this.model.account.name classNames="form-control" placeholder=(t 'editAccount.form.name.placeholder')}}
      {{else}}
        <div class="form-control-static">
          {{this.model.account.name}}
        </div>
      {{/if}}
    </div>
  </div>

  {{#if this.isAdmin}}
    <div class="row form-group">
      <div class="col-sm-12 col-md-3 form-label">
        <label class="form-control-static">{{t 'editAccount.form.kind.label'}}</label>
      </div>
      <div class="col-sm-12 col-md-7">
        {{new-select
          classNames="form-control"
          content=this.accountTypeChoices
          value=this.model.account.kind
          localizedLabel=true
        }}
      </div>
    </div>
  {{/if}}

  {{#if this.isAdmin}}
    <div class="row form-group">
      <div class="col-sm-12 col-md-3 form-label">
        <label for="description">{{t 'editAccount.form.description.label'}}</label>
      </div>
      <div class="col-sm-12 col-md-7">
        {{textarea value=this.model.account.description classNames="form-control no-resize" rows="2" placeholder=(t 'editAccount.form.description.placeholder')}}
      </div>
    </div>
  {{/if}}

  {{#if this.isLocalAuth}}
    <div class="row form-group">
      <div class="col-sm-12 col-md-3 form-label">
        <label class="form-control-static">{{t 'editAccount.form.publicValue.label'}}</label>
      </div>
      <div class="col-sm-12 col-md-7">
        <div class="form-control-static">
          {{this.model.credential.publicValue}}
        </div>
      </div>
    </div>

    {{#if this.needOld}}
      <div class="row form-group">
        <div class="col-sm-12 col-md-3 form-label">
          <label class="form-control-static">{{t 'editAccount.form.oldPassword.label'}}</label>
        </div>
        <div class="col-sm-12 col-md-7">
          {{input type="password" value=this.oldPassword classNames="form-control"}}
        </div>
      </div>
    {{/if}}

    <div class="row form-group">
      <div class="col-sm-12 col-md-3 form-label">
        <label class="form-control-static">{{t 'editAccount.form.newPassword.label'}}</label>
      </div>
      <div class="col-sm-12 col-md-7">
        {{schema/input-password value=this.newPassword generated="generated"}}
        <div class="help-block">{{this.validateDescription}}</div>
      </div>
    </div>

    {{#liquid-if this.showConfirm}}
      <div class="row form-group">
        <div class="col-sm-12 col-md-3 form-label">
          <label class="form-control-static">{{t 'editAccount.form.confirmPassword.label'}}</label>
        </div>
        <div class="col-sm-12 col-md-7">
          {{input type="password" value=this.newPassword2 classNames="form-control"}}
        </div>
      </div>
    {{/liquid-if}}
  {{else}}
    <div class="row form-group">
      <div class="col-sm-12 col-md-3 form-label">
        <label class="form-control-static">{{t 'editAccount.form.identity.label'}}</label>
      </div>
      <div class="col-sm-12 col-md-7">
        {{#if (and this.model.account.externalId this.model.account.externalIdType)}}
          {{identity-block externalIdType=this.model.account.externalIdType externalId=this.model.account.externalId}}
        {{else}}
          <div class="form-control-static text-muted">
            {{t 'generic.none'}}
          </div>
        {{/if}}
      </div>
    </div>
  {{/if}}
</section>

{{top-errors errors=this.errors}}
{{save-cancel editing=this.editing save="save" cancel="cancel"}}
