<h3>{{t 'siteAccess.header'}}</h3>
<p class="help-block">
  {{format-html-message 'siteAccess.helpText' appName=this.settings.appName}}
</p>
<hr/>

<div class="row">
  <div class="col-md-6">
    <div class="radio">
      <label>{{radio-button selection=this.copy.accessMode value="unrestricted"}} {{t 'siteAccess.unrestricted' individuals=(t this.individuals) collection=(t this.collection)}}</label>
    </div>
    <div class="radio">
      <label>{{radio-button selection=this.copy.accessMode value="restricted"}} {{t 'siteAccess.restricted' individuals=(t this.individuals) collection=(t this.collection)}}</label>
    </div>
    <div class="radio">
      <label>{{radio-button selection=this.copy.accessMode value="required"}} {{t 'siteAccess.required' individuals=(t this.individuals) collection=(t this.collection)}}</label>
    </div>
  </div>
  <div class="col-md-6">
    {{#if this.showList}}
      {{input-identity allowTeams=false action="addAuthorized" onError="gotError"}}

      <hr/>
      <h5>{{t 'siteAccess.listHeader' individuals=(t this.individuals) collection=(t this.collection)}}</h5>

      <ul class="list-unstyled gh-block-list r-mt10">
        {{#each this.copy.allowedIdentities as |item|}}
          <li>
            {{#identity-block identity=item}}
              <button class="btn btn-primary btn-sm pull-right gh-action" {{action "removeIdentity" item}} disabled={{eq item.id this.access.identity.id}}><i class="icon icon-minus"/></button>
            {{/identity-block}}
          </li>
        {{/each}}
      </ul>
      {{#if (eq this.copy.allowedIdentities.length 0)}}
        <span class="text-muted">{{t 'siteAccess.noIdentity'}}</span>
      {{/if}}
    {{/if}}
  </div>
</div>

<hr/>

{{top-errors errors=this.errors}}

<div class="inline-block">
  {{save-cancel cancelDisabled=true saved=this.saved savedDisabled=this.saved editing=true save="save"}}
</div>
