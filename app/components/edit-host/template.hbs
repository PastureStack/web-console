<section class="header">
  <h1>{{t 'editHost.title'}}</h1>
</section>
<section class="horizontal-form container-fluid">
  <div class="row form-group">
    <div class="col-sm-12 col-md-2 form-label">
      <label>{{t 'editHost.hostname.label'}}</label>
    </div>
    <div class="col-sm-12 col-md-8">
      {{this.model.hostname}}
      <p class="help-block">{{t 'editHost.hostname.help'}}</p>
    </div>
  </div>

  <div class="row form-group">
    <div class="col-sm-12 col-md-2 form-label form-control-static">
      <label>{{t 'editHost.customName.label'}}</label>
    </div>
    <div class="col-sm-12 col-md-8">
      {{input type="text" value=this.primaryResource.name classNames="form-control" placeholder=(t 'editHost.customName.placeholder')}}
      <p class="help-block">{{t 'editHost.customName.help'}}</p>
    </div>
  </div>

  <div class="row form-group">
    <div class="col-sm-12 col-md-2 form-label form-control-static">
      <label>{{t 'editHost.description.label'}}</label>
    </div>
    <div class="col-sm-12 col-md-8">
      {{textarea value=this.primaryResource.description classNames="form-control no-resize" rows="5" placeholder=(t 'editHost.description.placeholder')}}
    </div>
  </div>

  {{limit-parameters model=this.primaryResource}}

  <div class="row form-group">
    <div class="col-sm-12 col-md-2 form-label form-control-static">
      <label>{{t 'editHost.labels.label'}}</label>
    </div>
    <div class="col-sm-12 col-md-8">
      {{form-user-labels
         initialLabels=this.originalModel.labels
         readonlyLabels=this.readonlyLabels
         setLabels=(action 'setUserLabels')
      }}
    </div>
  </div>

  <div class="row form-group">
    <div class="col-sm-12 col-md-2 form-label form-control-static">
      <label>{{t 'editHost.ips.label'}}</label>
    </div>
    <div class="col-sm-12 col-md-8">
      {{form-value-array
        initialValues=this.ips
        changed=(fn (mut this.ips))
        addActionLabel="editHost.ips.addActionLabel"
        valueLabel="editHost.ips.valueLabel"
        valuePlaceholder="editHost.ips.valuePlaceholder"
      }}
      <p class="help-block">{{t 'editHost.ips.help'}}</p>
    </div>
  </div>

  <div class="row form-group">
    <div class="col-sm-12 col-md-2 form-label form-control-static">
      <label>{{t 'editHost.requireAny.label'}}</label>
    </div>
    <div class="col-sm-12 col-md-8">
      {{form-key-value
        initialStr=this.requireAny
        changedStr=(fn (mut this.requireAny))
        requiredIfAny=this.requiredIfAny
        addActionLabel="editHost.requireAny.addActionLabel"
        allowEmptyValue=true
      }}
      <p class="help-block">{{format-html-message 'editHost.requireAny.help'}}</p>
    </div>
  </div>
</section>

{{top-errors errors=this.errors}}
{{save-cancel editing=this.editing save="save" cancel="cancel"}}
