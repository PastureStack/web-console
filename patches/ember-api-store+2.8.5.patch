diff --git a/node_modules/ember-api-store/addon/index.js b/node_modules/ember-api-store/addon/index.js
index e52d626..186e25b 100644
--- a/node_modules/ember-api-store/addon/index.js
+++ b/node_modules/ember-api-store/addon/index.js
@@ -1,13 +1,7 @@
-import Ember from 'ember';
 import VERSION from 'ember-api-store/version';

-const EmberApiStore = Ember.Namespace.create({
+const EmberApiStore = Object.freeze({
   VERSION
 });

-if (Ember.libraries) {
-  Ember.libraries.registerCoreLibrary('Ember API Store', EmberApiStore.VERSION);
-}
-
 export default EmberApiStore;
-
diff --git a/node_modules/ember-api-store/addon/initializers/store.js b/node_modules/ember-api-store/addon/initializers/store.js
index b41b48b..6e9c401 100644
--- a/node_modules/ember-api-store/addon/initializers/store.js
+++ b/node_modules/ember-api-store/addon/initializers/store.js
@@ -1,3 +1,7 @@
+import Component from '@ember/component';
+import Controller from '@ember/controller';
+import Route from '@ember/routing/route';
+import { service } from '@ember/service';
 import Store from '../services/store';
 import Resource from '../models/resource';
 import Collection from '../models/collection';
@@ -19,8 +23,14 @@ export default function(serviceName='store', injectAs=null) {
       app.register('model:error', ApiError);
     }

-    app.inject('controller', injectAs, 'service:'+serviceName);
-    app.inject('route',      injectAs, 'service:'+serviceName);
-    app.inject('component',  injectAs, 'service:'+serviceName);
+    Controller.reopen({
+      [injectAs]: service(serviceName),
+    });
+    Route.reopen({
+      [injectAs]: service(serviceName),
+    });
+    Component.reopen({
+      [injectAs]: service(serviceName),
+    });
   };
 }
diff --git a/node_modules/ember-api-store/addon/instance-initializers/initialize-store-service.js b/node_modules/ember-api-store/addon/instance-initializers/initialize-store-service.js
index 07c0156..ce5873b 100644
--- a/node_modules/ember-api-store/addon/instance-initializers/initialize-store-service.js
+++ b/node_modules/ember-api-store/addon/instance-initializers/initialize-store-service.js
@@ -1,4 +1,9 @@
-export default function initializeStoreService(application) {
+export function initialize(application) {
   var container = application.lookup ? application : application.container;
   container.lookup('service:store');
 }
+
+export default {
+  name: 'initialize-store-service',
+  initialize,
+};
diff --git a/node_modules/ember-api-store/addon/mixins/serializable.js b/node_modules/ember-api-store/addon/mixins/serializable.js
index c3d4a51..3b03c1b 100644
--- a/node_modules/ember-api-store/addon/mixins/serializable.js
+++ b/node_modules/ember-api-store/addon/mixins/serializable.js
@@ -1,6 +1,9 @@
-import Ember from 'ember';
+import Mixin from '@ember/object/mixin';
+import { get } from '@ember/object';
+import { isArray } from '@ember/array';
+import { typeOf } from '@ember/utils';

-var Serializable = Ember.Mixin.create({
+var Serializable = Mixin.create({
   serialize: function(depth) {
     depth = depth || 0;
     var output;
@@ -10,7 +13,7 @@ var Serializable = Ember.Mixin.create({
       return null;
     }

-    if ( Ember.isArray(this) )
+    if ( isArray(this) )
     {
       output = this.map(function(item) {
         return recurse(item,depth+1);
@@ -33,7 +36,7 @@ var Serializable = Ember.Mixin.create({
         return null;
       }

-      if ( Ember.isArray(obj) )
+      if ( isArray(obj) )
       {
         return obj.map(function(item) {
           return recurse(item, depth+1);
@@ -69,7 +72,7 @@ var Serializable = Ember.Mixin.create({
     var out = Object.keys(this).filter((k) => {
       return k.charAt(0) !== '_' &&
         reserved.indexOf(k) === -1 &&
-        Ember.typeOf(Ember.get(this,k)) !== 'function';
+        typeOf(get(this,k)) !== 'function';
     });

     return out;
diff --git a/node_modules/ember-api-store/addon/mixins/type.js b/node_modules/ember-api-store/addon/mixins/type.js
index 721f184..d10c1ca 100644
--- a/node_modules/ember-api-store/addon/mixins/type.js
+++ b/node_modules/ember-api-store/addon/mixins/type.js
@@ -1,11 +1,13 @@
-import Ember from 'ember';
-import { get } from '@ember/object';
+import Mixin from '@ember/object/mixin';
+import { beginPropertyChanges, computed, endPropertyChanges, get } from '@ember/object';
+import { isArray } from '@ember/array';
+import { reject } from 'rsvp';
 import Serializable from './serializable';
 import { normalizeType } from '../utils/normalize';
 import { copyHeaders } from '../utils/apply-headers';
 import { urlOptions } from '../utils/url-options';

-var Type = Ember.Mixin.create(Serializable,{
+var Type = Mixin.create(Serializable,{
   id: null,
   type: null,
   links: null,
@@ -21,7 +23,7 @@ var Type = Ember.Mixin.create(Serializable,{
     newData.eachKeys(function(v, k) {
       if ( newData.hasOwnProperty(k) ) {
         var curVal = self.get(k);
-        if ( unionArrays && Ember.isArray(curVal) && Ember.isArray(v) ) {
+        if ( unionArrays && isArray(curVal) && isArray(v) ) {
           curVal.addObjects(v);
         } else {
           self.set(k, v);
@@ -116,7 +118,7 @@ var Type = Ember.Mixin.create(Serializable,{
   },

   computedHasAction: function(name) {
-    return Ember.computed('actionLinks.'+name, function() {
+    return computed('actionLinks.'+name, function() {
       return this.hasAction(name);
     });
   },
@@ -124,7 +126,7 @@ var Type = Ember.Mixin.create(Serializable,{
   doAction: function(name, data, opt) {
     var url = get(this, 'actionLinks.'+name);
     if (!url) {
-      return Ember.RSVP.reject(new Error('Unknown action: ' + name));
+      return reject(new Error('Unknown action: ' + name));
     }

     opt = opt || {};
@@ -152,7 +154,7 @@ var Type = Ember.Mixin.create(Serializable,{
     } else {
       // Create
       if ( !type ) {
-        return Ember.RSVP.reject(new Error('Cannot create record without a type'));
+        return reject(new Error('Cannot create record without a type'));
       }

       opt.method = opt.method || 'POST';
@@ -183,7 +185,7 @@ var Type = Ember.Mixin.create(Serializable,{
       var newId = newData.get('id');
       var newType = normalizeType(newData.get('type'));
       if ( !id && newId && type === newType ) {
-        Ember.beginPropertyChanges();
+        beginPropertyChanges();

         // A new record was created.  Typeify will have put it into the store,
         // but it's not the same instance as this object.  So we need to fix that.
@@ -207,7 +209,7 @@ var Type = Ember.Mixin.create(Serializable,{
           }
         }

-        Ember.endPropertyChanges();
+        endPropertyChanges();
       }

       return self;
@@ -233,7 +235,7 @@ var Type = Ember.Mixin.create(Serializable,{

   reload: function(opt) {
     if ( !this.hasLink('self') ) {
-      return Ember.RSVP.reject('Resource has no self link');
+      return reject('Resource has no self link');
     }

     var url = this.linkFor('self');
diff --git a/node_modules/ember-api-store/addon/models/collection.js b/node_modules/ember-api-store/addon/models/collection.js
index bbb9795..37d6fbc 100644
--- a/node_modules/ember-api-store/addon/models/collection.js
+++ b/node_modules/ember-api-store/addon/models/collection.js
@@ -1,11 +1,11 @@
-import Ember from 'ember';
+import ArrayProxy from '@ember/array/proxy';
+import { getOwner } from '@ember/application';
+import { Promise } from 'rsvp';
 import TypeMixin from '../mixins/type';
 import { copyHeaders } from '../utils/apply-headers';
 import { normalizeType } from '../utils/normalize';

-const { getOwner } = Ember;
-
-export default Ember.ArrayProxy.extend(TypeMixin, {
+export default ArrayProxy.extend(TypeMixin, {
   type: 'collection',
   createDefaults: null,
   createTypes: null,
@@ -44,13 +44,13 @@ export default Ember.ArrayProxy.extend(TypeMixin, {
     if ( depth > 5 )
     {
       console.log('Depaginate, max depth reached');
-      return new Ember.RSVP.Promise(function(resolve,reject) {
+      return new Promise(function(resolve,reject) {
         resolve();
       });
     }
     */

-    var promise = new Ember.RSVP.Promise(function(resolve,reject) {
+    var promise = new Promise(function(resolve,reject) {
       var next = self.get('pagination.next');
       if ( next )
       {
diff --git a/node_modules/ember-api-store/addon/models/resource.js b/node_modules/ember-api-store/addon/models/resource.js
index c3eb71b..81240ba 100644
--- a/node_modules/ember-api-store/addon/models/resource.js
+++ b/node_modules/ember-api-store/addon/models/resource.js
@@ -1,6 +1,7 @@
-import Ember from 'ember';
+import EmberObject from '@ember/object';
+import { ActionHandler } from '@ember/-internals/runtime';
 import TypeMixin from '../mixins/type';
-import { set, get } from '@ember/object';
+import { computed, set, get } from '@ember/object';
 import { isArray } from '@ember/array';
 import { displayKeyFor, validateLength, validateChars, validateHostname, validateDnsLabel } from '../utils/validate';
 import { normalizeType } from '../utils/normalize';
@@ -17,7 +18,7 @@ const STRING_LIKE_TYPES = [
   'hostname',
 ];

-var Actionable = Ember.Object.extend(Ember.ActionHandler);
+var Actionable = EmberObject.extend(ActionHandler);
 var Resource = Actionable.extend(TypeMixin, {
   // You should probably override intl with a real translator...
   intl: {
@@ -46,10 +47,10 @@ var Resource = Actionable.extend(TypeMixin, {
     return data;
   },

-  schema: function() {
+  schema: computed('type', function() {
     const schema = this.get('store').getById('schema', this.get('type'));
     return schema;
-  }.property('type'),
+  }),

   validationErrors() {
     const intl = this.get('intl');
diff --git a/node_modules/ember-api-store/addon/models/schema.js b/node_modules/ember-api-store/addon/models/schema.js
index 06c6047..524b42a 100644
--- a/node_modules/ember-api-store/addon/models/schema.js
+++ b/node_modules/ember-api-store/addon/models/schema.js
@@ -1,6 +1,6 @@
 import Resource from './resource';
 import { normalizeType } from '../utils/normalize';
-import { get, set } from '@ember/object';
+import { computed, get, set } from '@ember/object';

 export const SCHEMA = {
   SIMPLE: ['string','password','masked','multiline','float','int','date','blob','boolean','enum','reference','json','dnsLabel','hostname','dnsLabelRestricted'],
@@ -16,7 +16,7 @@ var Schema = Resource.extend({
     return Object.keys(get(this, 'resourceFields'));
   },

-  typeifyFields: function() {
+  typeifyFields: computed(function() {
     // Schemas are special..
     if ( get(this, 'id') === 'schema' ) {
       return [];
@@ -37,7 +37,7 @@ var Schema = Resource.extend({
     });

     return out;
-  }.property(),
+  }),

   getLoadDefaults(record, forceOverwrite=false) {
     var fields = get(this, 'resourceFields');
diff --git a/node_modules/ember-api-store/addon/services/store.js b/node_modules/ember-api-store/addon/services/store.js
index 7e80cdf..22e33a4 100644
--- a/node_modules/ember-api-store/addon/services/store.js
+++ b/node_modules/ember-api-store/addon/services/store.js
@@ -1,28 +1,20 @@
-import Ember from 'ember';
+import Service, { service } from '@ember/service';
+import ArrayProxy from '@ember/array/proxy';
+import { getOwner, setOwner } from '@ember/application';
 import Serializable from '../mixins/serializable';
 import ApiError from '../models/error';
 import { normalizeType } from '../utils/normalize';
 import { applyHeaders } from '../utils/apply-headers';
 import fetch from 'ember-api-store/utils/fetch';
 import { urlOptions } from '../utils/url-options';
-import { get, set } from '@ember/object';
-import { reject } from 'rsvp';
-import { inject as service } from '@ember/service';
+import { beginPropertyChanges, computed, endPropertyChanges, get, getProperties, set } from '@ember/object';
+import { defer, reject, resolve } from 'rsvp';
 import { isArray } from '@ember/array';
 import { parse as setCookieParser } from 'set-cookie-parser';

-const { getOwner } = Ember;
-
-function getOwnerKey() {
-  const x = {};
-  Ember.setOwner(x);
-  return Object.keys(x)[0];
-}
-const ownerKey = getOwnerKey();
-
 export const defaultMetaKeys = ['actionLinks','createDefaults','createTypes','filters','links','pagination','resourceType','sort','sortLinks','type'];

-var Store = Ember.Service.extend({
+var Store = Service.extend({
   cookies: service(),

   defaultTimeout: 30000,
@@ -33,7 +25,7 @@ var Store = Ember.Service.extend({
   dropKeys: null,
   headers: null,

-  arrayProxyClass: Ember.ArrayProxy,
+  arrayProxyClass: ArrayProxy,
   arrayProxyKey: 'content',
   arrayProxyOptions: null,

@@ -41,8 +33,8 @@ var Store = Ember.Service.extend({
   removeAfterDelete: true,


-  fastboot: Ember.computed(function() {
-    return Ember.getOwner(this).lookup('service:fastboot');
+  fastboot: computed(function() {
+    return getOwner(this).lookup('service:fastboot');
   }),

   init() {
@@ -158,7 +150,7 @@ var Store = Ember.Service.extend({
     }

     if ( !type ) {
-      return Ember.RSVP.reject(ApiError.create({detail: 'type not specified'}));
+      return reject(ApiError.create({detail: 'type not specified'}));
     }

     // If this is a request for all of the items of [type], then we'll remember that and not ask again for a subsequent request
@@ -168,11 +160,11 @@ var Store = Ember.Service.extend({
     // See if we already have this resource, unless forceReload is on.
     if ( opt.forceReload !== true ) {
       if ( opt.isForAll && this._state.foundAll[type] ) {
-        return Ember.RSVP.resolve(this.all(type),'Cached find all '+type);
+        return resolve(this.all(type),'Cached find all '+type);
       } else if ( isCacheable && id ) {
         var existing = this.getById(type,id);
         if ( existing ) {
-          return Ember.RSVP.resolve(existing,'Cached find '+type+':'+id);
+          return resolve(existing,'Cached find '+type+':'+id);
         }
       }
     }
@@ -191,7 +183,7 @@ var Store = Ember.Service.extend({
           }
         }

-        return Ember.RSVP.reject(ApiError.create({detail: 'Unable to find schema for "' + type + '"'}));
+        return reject(ApiError.create({detail: 'Unable to find schema for "' + type + '"'}));
       });
     }
   },
@@ -214,7 +206,7 @@ var Store = Ember.Service.extend({
     opt = opt || {};

     if ( this.haveAll(type) && this.isCacheable(opt) ) {
-      return Ember.RSVP.resolve(this.all(type),'All '+ type + ' already cached');
+      return resolve(this.all(type),'All '+ type + ' already cached');
     } else {
       return this.find(type, undefined, opt).then(() => {
         return this.all(type);
@@ -445,9 +437,9 @@ var Store = Ember.Service.extend({
     if (queue[queueKey]) {
       // get the filterd promise object
       var filteredPromise = queue[queueKey];
-      let defer = Ember.RSVP.defer();
-      filteredPromise.push(defer);
-      later = defer.promise;
+      let deferred = defer();
+      filteredPromise.push(deferred);
+      later = deferred.promise;

     } else { // request is not in the findQueue

@@ -477,7 +469,7 @@ var Store = Ember.Service.extend({
         return result;
       }, (reason) => {
         this._finishFind(queueKey, reason, 'reject');
-        return Ember.RSVP.reject(reason);
+        return reject(reason);
       });

       // set the queue array to empty indicating we've had 1 promise already
@@ -513,11 +505,11 @@ var Store = Ember.Service.extend({
     }

     if ( xhr.body && typeof xhr.body === 'object' ) {
-      Ember.beginPropertyChanges();
+      beginPropertyChanges();
       let response = this._typeify(xhr.body);
       delete xhr.body;
       Object.defineProperty(response, 'xhr', {value: xhr, configurable: true});
-      Ember.endPropertyChanges();
+      endPropertyChanges();

       // Depaginate
       if ( opt.depaginate && typeof response.depaginate === 'function' ) {
@@ -555,9 +547,9 @@ var Store = Ember.Service.extend({

       return finish(body);
     } else if ( xhr.body && typeof xhr.body === 'object' ) {
-      Ember.beginPropertyChanges();
+      beginPropertyChanges();
       let out = finish(this._typeify(xhr.body));
-      Ember.endPropertyChanges();
+      endPropertyChanges();

       return out;
     } else {
@@ -576,7 +568,7 @@ var Store = Ember.Service.extend({

       delete xhr.body;
       Object.defineProperty(body, 'xhr', {value: xhr, configurable: true});
-      return Ember.RSVP.reject(body);
+      return reject(body);
     }
   },

@@ -765,7 +757,7 @@ var Store = Ember.Service.extend({
       opt = {applyDefaults: false};
     }

-    let type = Ember.get(input,'type');
+    let type = get(input,'type');
     if ( isArray(input) ) {
       // Recurse over arrays
       return input.map(x => this._typeify(x, opt));
@@ -854,7 +846,7 @@ var Store = Ember.Service.extend({

   // Create a collection: {key: 'data'}
   createCollection(input, opt) {
-    Ember.beginPropertyChanges();
+    beginPropertyChanges();
     let key = (opt && opt.key ? opt.key : 'data');
     var cls = getOwner(this).lookup('model:collection');
     var content = input[key].map(x => this._typeify(x, opt));
@@ -862,8 +854,8 @@ var Store = Ember.Service.extend({

     Object.defineProperty(output, 'store', { value: this, configurable: true });

-    output.setProperties(Ember.getProperties(input, get(this, 'metaKeys')));
-    Ember.endPropertyChanges();
+    output.setProperties(getProperties(input, get(this, 'metaKeys')));
+    endPropertyChanges();
     return output;
   },

@@ -899,7 +891,7 @@ var Store = Ember.Service.extend({
   // Create a record: {applyDefaults: false}
   createRecord(data, opt) {
     opt = opt || {};
-    let type = normalizeType(Ember.get(opt,'type')||Ember.get(data,'type')||'', this);
+    let type = normalizeType(get(opt,'type')||get(data,'type')||'', this);

     let cls;
     if ( type ) {
@@ -935,7 +927,7 @@ var Store = Ember.Service.extend({
       }
     }
     var output = cons.create(input);
-    Object.defineProperty(output, ownerKey, {enumerable: false, value: Ember.getOwner(this)})
+    setOwner(output, getOwner(this));

     Object.defineProperty(output, 'store', {enumerable: false, value: this, configurable: true});
     return output;
diff --git a/node_modules/ember-api-store/addon/utils/ajax-promise.js b/node_modules/ember-api-store/addon/utils/ajax-promise.js
index 2bd6e67..96fa477 100644
--- a/node_modules/ember-api-store/addon/utils/ajax-promise.js
+++ b/node_modules/ember-api-store/addon/utils/ajax-promise.js
@@ -1,8 +1,9 @@
-import Ember from 'ember';
+import $ from 'jquery';
+import { Promise } from 'rsvp';

 export function ajaxPromise(opt, justBody) {
-  var promise = new Ember.RSVP.Promise(function(resolve,reject) {
-    Ember.$.ajax(opt).then(success,fail);
+  var promise = new Promise(function(resolve,reject) {
+    $.ajax(opt).then(success,fail);

     function success(body, textStatus, xhr) {
       if ( justBody === true )
diff --git a/node_modules/ember-api-store/addon/utils/apply-headers.js b/node_modules/ember-api-store/addon/utils/apply-headers.js
index 1fb181d..d634010 100644
--- a/node_modules/ember-api-store/addon/utils/apply-headers.js
+++ b/node_modules/ember-api-store/addon/utils/apply-headers.js
@@ -1,4 +1,4 @@
-import Ember from 'ember';
+import { get } from '@ember/object';

 // Copy the headers from `more` into the object `dest`
 // including ones with a value of undefined, so they
@@ -10,7 +10,7 @@ export function copyHeaders(more, dest) {
   }

   Object.keys(more).forEach(function(key) {
-    var val = Ember.get(more, key);
+    var val = get(more, key);
     var normalizedKey = key.toLowerCase();
     dest[normalizedKey] = val;
   });
@@ -24,7 +24,7 @@ export function applyHeaders(more, dest, copyUndefined) {
   }

   Object.keys(more).forEach(function(key) {
-    var val = Ember.get(more, key);
+    var val = get(more, key);
     var normalizedKey = key.toLowerCase();
     if ( val === undefined && copyUndefined !== true )
     {
diff --git a/node_modules/ember-api-store/addon/utils/denormalize.js b/node_modules/ember-api-store/addon/utils/denormalize.js
index a74c90e..d636423 100644
--- a/node_modules/ember-api-store/addon/utils/denormalize.js
+++ b/node_modules/ember-api-store/addon/utils/denormalize.js
@@ -1,4 +1,4 @@
-import Ember from 'ember';
+import { computed } from '@ember/object';
 import { normalizeType } from '../utils/normalize';

 function _getReference(store, referencedType, referencedId, thisType, thisId, computedKey) {
@@ -53,7 +53,7 @@ export function reference(field, referencedType=null, storeName="store") {
     referencedType = field.replace(/Id$/,'');
   }

-  return Ember.computed(field, {
+  return computed(field, {
     get(computedKey) {
       const store = this.get(storeName);
       const thisType = this.get('type');
@@ -71,7 +71,7 @@ export function arrayOfReferences(field=null, referencedType=null, storeName="st
     referencedType = field.replace(/Id$/,'');
   }

-  return Ember.computed(field+'.[]', {
+  return computed(field+'.[]', {
     get(computedKey) {
       const store = this.get(storeName);
       const thisType = this.get('type');
@@ -96,7 +96,7 @@ export function arrayOfReferences(field=null, referencedType=null, storeName="st
 export function hasMany(matchField, targetType, targetField, storeName="store", additionalFilter=null, sourceStoreName=null) {
   targetType = normalizeType(targetType);

-  return Ember.computed({
+  return computed({
     get(computedKey) {
       let store = this.get(storeName);
       let sourceStore;
@@ -133,3 +133,8 @@ export function hasMany(matchField, targetType, targetField, storeName="store",
     }
   });
 }
+
+// Compatibility aliases retained by the 1.6 UI models.  ember-api-store
+// renamed these helpers without keeping the former public exports.
+export const denormalizeId = reference;
+export const denormalizeIdArray = arrayOfReferences;
diff --git a/node_modules/ember-api-store/addon/utils/url-options.js b/node_modules/ember-api-store/addon/utils/url-options.js
index 857e06d..3604d61 100644
--- a/node_modules/ember-api-store/addon/utils/url-options.js
+++ b/node_modules/ember-api-store/addon/utils/url-options.js
@@ -1,4 +1,4 @@
-import Ember from 'ember';
+import { isArray } from '@ember/array';

 export function urlOptions(url,opt,cls) {
   opt = opt || {};
@@ -10,7 +10,7 @@ export function urlOptions(url,opt,cls) {
     var keys = Object.keys(opt.filter);
     keys.forEach(function(key) {
       var vals = opt.filter[key];
-      if ( !Ember.isArray(vals) )
+      if ( !isArray(vals) )
       {
         vals = [vals];
       }
