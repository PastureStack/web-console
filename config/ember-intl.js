// Translation YAML is converted into side-loadable JSON by
// lib/translation-json-tree.js. Keep this neutral metadata for tooling that
// discovers the historical Ember Intl configuration file.
module.exports = function() {
  return {
    baseLocale: 'en-us',
    inputPath: 'translations',
    outputPath: 'translations'
  };
};
