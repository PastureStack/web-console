#!/usr/bin/env bash
set -euo pipefail

cat >&2 <<'MSG'
The historical legacy Web Console build entrypoint is disabled in the PastureStack migration source tree.

It depended on the legacy Node/Bower/node-sass toolchain. Do not rebuild or publish a UI artifact from this path. Use docs/modernization.md to migrate the build chain first.
MSG
exit 2
