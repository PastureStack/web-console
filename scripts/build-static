#!/usr/bin/env bash
set -euo pipefail

cat >&2 <<'MSG'
The historical legacy Web Console static artifact builder is disabled in the PastureStack migration source tree.

The currently embedded server artifact remains at the preserved compatibility version. A new artifact must only be published after the build chain is modernized and PastureStack Server browser/API regression tests pass.
MSG
exit 2
