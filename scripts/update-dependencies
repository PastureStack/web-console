#!/usr/bin/env bash
set -euo pipefail

cat >&2 <<'MSG'
The historical legacy Web Console dependency installer is disabled in the PastureStack migration source tree.

It relied on Bower and the old Node/node-sass stack. Add a reproducible modern dependency path before installing or updating UI dependencies.
MSG
exit 2
